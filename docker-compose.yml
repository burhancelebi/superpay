services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: superpay
        working_dir: /var/www/html
        tty: true
        restart: unless-stopped
        volumes:
            - ./:/var/www/html
            - ./docker/php/php.ini:/usr/local/etc/php/php.ini
        networks:
            - superpay

    #Nginx Service
    nginx:
        image: nginx:latest
        container_name: pay_nginx
        ports:
            - "8002:8002"
            - "443:443"
        volumes:
            - ./:/var/www/html
            - ./docker/nginx/local.conf:/etc/nginx/conf.d/app.conf
            - ./docker/nginx/ssl:/etc/nginx/ssl
        depends_on:
            - web
            - mysql
        networks:
            - superpay

    #MySQL Service


    mysql:
        image: mysql:8
        container_name: mysql
        restart: unless-stopped
        platform: linux/x86_64
        tty: true
        volumes:
            - mysql-data:/var/lib/mysql
            - mysql-log:/var/log/mysql
        ports:
            - "3306:3306"
        environment:
            MYSQL_DATABASE: ${DB_DATABASE}
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
            MYSQL_PASSWORD: ${DB_PASSWORD}
            MYSQL_ROOT_HOST: '%'
            MYSQL_USER: ${DB_USERNAME}
            SERVICE_TAGS: dev
            SERVICE_NAME: mysql
        networks:
            - superpay

    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        restart: always
        ports:
            - "8081:80"
        environment:
            PMA_HOST: mysql
            #PMA_USER: root
            #PMA_PASSWORD: ${DB_PASSWORD}
        networks:
            - superpay
        depends_on:
            - mysql

#Docker Networks
networks:
    superpay:
        driver: bridge

volumes:
    mysql-log:
        driver: local
    mysql-data:
        driver: local
